const express = require('express');
const db = require('../db');
const router = express.Router();

router.post('/', (req, res) => {
  //TODO: takes a product information and add the product in database
});

module.exports = router;