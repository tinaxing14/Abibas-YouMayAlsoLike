const express = require('express');
const db = require('../db');
const router = express.Router();

router.delete('/', (req, res) => {
  //TODO: takes a product id add delete the product in database
});

module.exports = router;